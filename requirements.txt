torch>=2.1.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
pyyaml>=6.0
tqdm>=4.65.0
pytest>=7.4.0
huggingface_hub>=0.21.0

# ── Optional (strongly recommended) 

# Real Mamba2 CUDA kernels (much faster on GPU than NativeMambaBlock)
# Requires CUDA 11.8+ and the correct torch version:
#   pip install mamba-ssm causal-conv1d
# mamba-ssm>=2.0.4

# Experiment tracking
# wandb>=0.16.0

# LoRA fine-tuning (for foundation model adaptation)
# peft>=0.8.0

# Mixed precision training (built into torch, no extra install needed)
#torch.cuda.amp  #is used automatically via GradScaler in trainer
